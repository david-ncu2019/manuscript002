% \section{Methodology}

\subsection{SBAS-InSAR time series processing}
\label{subsec:sbas}

This study derived cumulative vertical displacement from an 11-year dataset of Sentinel-1 SAR images \text{(2015 - 2025)}, acquired from both ascending and descending orbits; the image properties are summarized in \Cref{tab:sentinel1_info}. First, the images from each acquisition orbit were processed separately applying the \textbf{\textit{hyp3-isce2}} plugin, part of the Hybrid Pluggable Processing Pipeline (HyP3) \citep{hogenson_hybrid_2025}. For interferogram formation, each SAR image was paired with up to four subsequent consecutive images to minimize temporal decorrelation. Next, each image pair was analyzed using the InSAR Scientific Computing Environment 2 (ISCE2) TOPS workflow, which sequentially performs burst-level coregistration of Single Look Complex (SLC) images, interferogram generation, waterbody masking and topographic phase correction with the Copernicus GLO-30 DEM, phase unwrapping via the SNAPHU algorithm, and geocoding to produce unwrapped interferograms suitable for further time-series InSAR analysis. A detailed description of the ISCE2 workflow is available in \citep{rosen_insar_2012, yague-martinez_interferometric_2016, fattahi_network-based_2017}.

Subsequently, a time-series analysis was conducted on the unwrapped interferograms from ascending and descending orbits separately using the small baseline subset approach, provided by Miami InSAR Time-series software (MintPy) \citep{yunjun_small_2019}. The valid interferogram network was formed through a two-stage selection. The minimum spanning tree (MST) \citep{perissin_repeat-pass_2012} first identified the most coherent interferograms to connect all SAR images, by using the inverse of the average spatial coherence of all interferograms as weight. After this, any interferograms not included in the MST were excluded if their average spatial coherence was lower than $\text{0.3}$. After network formation, the optimal values of the interferometric phase timeseries were estimated through a network inversion using the inverse of the phase variance as weight \citep{tough_statistical_1995}. The temporal coherence, as a product of the network inversion process, was utilized to evaluate the reliability of the estimated value at each pixel \citep{pepe_extension_2006}. Pixels with temporal coherence ($\gamma_\textit{temp}$) below 0.65 were then masked from further processing. Regarding atmospheric correction, the tropospheric delay components were removed based on the empirical linear relationship between InSAR phase delay and elevation \citep{doin_corrections_2009}. All interferograms from both ascending and descending orbits were referenced to a single stable point, which was located far from the subsiding area (\Cref{fig:insar_leveling_a}). Finally, the line-of-sight deformation (${d_\textit{LOS}}$) was decomposed into the east-west (${d_\textit{E-W}}$) and vertical (${d_\textit{U}}$) components, assuming the deformation in north-south direction (${d_\textit{N-S}}$) was negligible. The decomposition employed the incidence angles ($\theta$) and satellite heading angles ($\alpha$) from both ascending ($\textit{asc}$) and descending ($\textit{desc}$) images \citep{hanssen_radar_2001}:

\begin{equation}
	\begin{bmatrix}
		d_{\textit{U}} \\
		d_{\textit{E-W}}
	\end{bmatrix}
	=
	\begin{bmatrix}
		\cos(\theta^{\text{asc}}) & \sin(\theta^{\text{asc}}) \cos(\alpha^{\text{asc}}) \\
		\cos(\theta^{\text{desc}}) & \sin(\theta^{\text{desc}}) \cos(\alpha^{\text{desc}})
	\end{bmatrix}^{-1}
	\begin{bmatrix}
		d_{\textit{LOS}}^{\text{asc}} \\
		d_{\textit{LOS}}^{\text{desc}}
	\end{bmatrix}.
	\label{eq:decomposition}
\end{equation}

Validation of the vertical displacements was performed against precise leveling survey, with benchmarks locations shown in \Cref{fig:studyarea}. First, reliable measurement points ($\gamma_\textit{temp} \ge 0.65$) within a 200 m radius of each benchmark were selected. Average velocities were then derived for these points from both the InSAR and annual leveling survey time series, calculated as the slope of the best-fitting line to their respective displacement time series. Such a comparison based on rates was required due to the different temporal sampling intervals of the two datasets \Cref{fig:insar_leveling}.

\begin{figure}[H]
	\centering
	\begin{subfigure}[b]{0.3\textwidth}
		\centering
		\framebox{\rule{0pt}{8cm}\rule{\textwidth}{0pt}}
		\caption{}
		\label{fig:insar_leveling_a}
	\end{subfigure}
	\quad
	\begin{subfigure}[b]{0.3\textwidth}
		\centering
		\framebox{\rule{0pt}{8cm}\rule{\textwidth}{0pt}}
		\caption{}
		\label{fig:insar_leveling_b}
	\end{subfigure}
	\caption{Comparison of InSAR results and leveling data. (a) The results from InSAR processing. (b) The ground truth data from leveling surveys.}
	\label{fig:insar_leveling}
\end{figure}

\subsection{Borehole Lithology Transformation}
\label{subsec:borehole_ilr}
% Manuscript placeholder: Describe how borehole material logs were converted into
% section-level compositional predictors. The method should define the six 50 m
% sections S1--S6, explain the material grouping used for sand/silt/clay or coarse
% versus fine fractions, and state why raw fractions were transformed before model
% fitting.
% Manuscript placeholder: Include the Sequential Binary Partition and Isometric
% Log-Ratio transformation used to avoid treating compositional fractions as
% independent Euclidean variables. Keep the prose procedural and avoid claiming
% transfer skill from static lithology alone, because run_032 improved at-well
% scores but did not improve LOSO transfer on average.
% Evidence anchors:
% - 014_ml_nowcast/raw_data/05_borehole_materials/
% - 014_ml_nowcast/docs/20260702_report_after_feature_refine.md
% - 014_ml_nowcast/docs/20260711_ML_features_v4_extended.md

\subsection{Bayesian Ridge Regression (BRR) Nowcasting}
\label{sec:brr_nowcasting}
% Manuscript placeholder: Write the Bayesian Ridge Regression formulation for
% monthly at-well nowcasting of per-section compaction increments. Define one row
% as month x 50 m depth section, define the target as y_t = MLCW_section.diff()
% in mm/month, and state the sign convention that negative y_t indicates
% compaction.
% Manuscript placeholder: Describe the active feature family around run_035:
% same-month dGWL, d2GWL, absolute dGWL, explicit dGWL lags from 1 to 36 months,
% 3- and 6-month rolling head-change means, rainfall and antecedent rainfall
% sums, semiannual seasonality, cross-section GWL features, ILR lithology
% coordinates, and GNSS-derived dS_total and d2S_total. State that this is
% nowcasting, not forecasting, because the feature row uses same-month observable
% inputs.
% Manuscript placeholder: Keep the interpretation bounded. BRR coefficients
% describe predictive associations under regularization and do not physically
% decompose the rank-1 surface displacement carrier into exact layer contributions.
% Evidence anchors:
% - 014_ml_nowcast/README.md
% - 014_ml_nowcast/STATUS.md
% - 014_ml_nowcast/docs/20260711_ML_features_v4_extended.md

\subsection{Temporal and Spatial Cross-Validation}
\label{sec:cross_validation}
% Manuscript placeholder: Describe the temporal validation protocol as an
% expanding-window or walk-forward evaluation, depending on the final run selected
% for manuscript reporting. Distinguish calendar test-span length from forecast
% horizon: each prediction remains a same-month nowcast.
% Manuscript placeholder: Describe the spatial-transfer validation protocol as
% leave-one-station-out cross-validation. State that the model is trained on the
% remaining stations and scored on the held-out MLCW station, with S1--S4 reported
% separately from S5--S6 where sensor coverage and slow consolidation limit skill.
% Evidence anchors:
% - 014_ml_nowcast/docs/20260702_report_after_feature_refine.md
% - 014_ml_nowcast/docs/20260711_ML_features_v4_extended.md
% - 014_ml_nowcast/CHANGE_NOTES/20260708_run043_run044_gwl_v5_results.md

\subsection{Split-Conformal Prediction Intervals}
\label{sec:uncertainty}
% Manuscript placeholder: Describe the split-conformal prediction interval
% procedure used for non-parametric uncertainty around BRR nowcasts. Define the
% calibration residuals, the target nominal coverage, and the resulting lower and
% upper interval bounds for monthly compaction increments.
% Manuscript placeholder: Explain why split-conformal intervals are reported
% instead of relying only on parametric posterior variance: the interval should
% reflect held-out residual behavior from the nowcast pipeline. If horizon-binned
% intervals are used in the final result set, specify the calibration grouping and
% the exact nominal coverage used by the diagnostic.
% Evidence anchors:
% - 014_ml_nowcast/README.md
% - 014_ml_nowcast/scripts/05_train_nowcast.py
% - 014_ml_nowcast/diagnostics/scripts/diag_15_horizon_conformal.py
