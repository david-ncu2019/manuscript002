# MS2 Dataset Section Edit Notes

Created: 2026-08-08 18:24:45 +08:00

Target section: `sections/dataset003.tex`

Working draft: `sections/dataset003_temp.tex` after the first change set is approved

This file records only issues observed in the active manuscript. It does not predict future problems or authorize manuscript changes. Each change still requires author approval before it is applied.

## Status Terms

- **Open:** the issue has been observed but no change has been approved.
- **Approved for temporary draft:** the proposed change may be written to `dataset003_temp.tex`.
- **Resolved in temporary draft:** the change has been applied and checked in the temporary PDF.
- **Approved for baseline:** the temporary wording may replace the corresponding baseline text.
- **Closed without change:** the issue was reviewed and the existing text was retained.

## DS-001: The Dataset Opening Performs Too Many Jobs

**Status:** Resolved in temporary draft

**Location:** `sections/dataset003.tex`, opening paragraph under `\subsection{Datasets}`

The paragraph introduces the monitoring system, explains delayed data availability, defines the six-month evaluation setting, selects the Tuku station, lists four datasets, describes their model roles, states the evaluation procedure, distinguishes the task from autoregressive prediction, reports observation periods, and introduces the child subsections. These functions create more than one narrative arc within a single paragraph.

The opening should retain the information needed to understand why the datasets were assembled. Detailed evaluation steps already described in the Introduction and Methodology should not dominate the Dataset opening.

**Next action:** Prepare an approval-gated change set that separates the delayed-availability explanation from the dataset inventory while preserving all confirmed facts.

## DS-002: Data Availability and Measurement Frequency Need a Clear Distinction

**Status:** Resolved in temporary draft

**Location:** `sections/dataset003.tex`, sentences describing provider processing and the six-month information gap

The current wording states that field measurements were collected monthly and finalized records became available later. International readers may still interpret the delay as less frequent field measurement. The Methodology confirms that each delayed block contains the complete sequence of monthly MLCW observations after the block ends.

**Next action:** Add a short explanation that the delay changes when the monthly observations can be used, not how often deformation is measured. This addition is grounded in `sections/methods005.tex`, `\subsubsection{Delayed data delivery}`.

## DS-003: Evaluation Details Overlap with the Methodology

**Status:** Open

**Location:** `sections/dataset003.tex`, opening paragraph; related text in `sections/methods005.tex`, `\subsection{Experimental design}`

The Dataset opening explains withholding six months of MLCW observations, comparing estimates with finalized records, excluding earlier compaction values, and treating the interval as an evaluation setting. The Methodology provides the full experimental design and update sequence. Repeating these details in both places weakens the role of the Dataset section.

**Why resolution is limited now:** Cross-section integration is not the current priority. The Dataset section should be made internally clear first.

**Next action:** Retain only the minimum overview needed to understand data availability. Leave the detailed evaluation sequence in the Methodology. Review the Dataset-to-Methods boundary later.

**Temporary-draft update:** The opening now retains only a short description of the six-month information gap. The complete evaluation and update sequence remains in the Methodology. The broader Dataset-to-Methods overlap remains open for later review.

## DS-004: The Reduced-Sampling Cross-Reference Does Not Point to the Active Section

**Status:** Resolved in temporary draft

**Location:** `sections/dataset003.tex`, `\Cref{subsec:discussion_reduced_sampling}` in the opening paragraph

The active file included by `main.tex` is `sections/results_discussion_draft.tex`, where the corresponding subsection is labelled `subsec:results_reduced_frequency`. The label used in the Dataset opening belongs to older Results or Discussion files that are not part of the active manuscript path.

**Next action:** Decide whether the Dataset opening needs this forward reference. If it remains useful, update it to the active label after confirming the final Results and Discussion structure. Otherwise, remove it from the Dataset opening. Do not change it automatically while that section remains a draft.

## DS-005: Dataset Roles Are Repeated at the Beginning and End

**Status:** Open

**Location:** opening paragraph and the paragraph beginning `Together, the four datasets`

Both passages explain that MLCW provides section-level compaction, GWL and cGNSS provide time-varying information, and lithology distinguishes depth sections. The repetition makes the closing synthesis less effective.

**Next action:** Let the opening identify the datasets and the immediate analysis setting. Let the closing paragraph synthesize how the completed dataset supports the Methodology. Check the two passages together before either is approved.

## DS-006: Figure 2 Has No Current In-Text Introduction

**Status:** Open

**Location:** `sections/dataset003.tex`, figure labelled `fig:tuku_observations`

The three-panel observational figure appears after the four dataset descriptions, but no active sentence introduces the figure or tells readers what relationship to inspect. The manuscript should not add a generic figure announcement merely to satisfy a cross-reference check.

**Next action:** Keep this issue open until the scientific purpose of the completed panels is clear. Add a content-bearing connection only after the figure content is available or the author supplies the intended message.

## Review Order

1. Dataset opening and delayed-availability explanation.
2. MLCW observations.
3. GWL observations.
4. cGNSS observations.
5. Borehole lithology.
6. Closing synthesis, figures, and table references.

Each block requires a separate approved change set before it is written to the temporary LaTeX file.

## Approved Change Set 1: Dataset Opening

**Author decision:** Option B with two corrections was approved.

The temporary draft separates the delayed-availability explanation from the dataset inventory. It explicitly states that delayed provision affected data availability rather than measurement frequency. The phrase `section-level` was replaced with `individual depth intervals`, and the repeated statement that the lithological profile accompanied the MLCW records was removed. Detailed comparison and model-update steps were left in the Methodology, and the inactive reduced-sampling cross-reference was removed from the Dataset opening.

**Temporary file:** `sections/dataset003_temp.tex`

**Active review input:** `main.tex` currently inputs `sections/dataset003_temp.tex`.

**Build result:** The manual `pdflatex`, `bibtex`, `pdflatex`, `pdflatex` sequence completed successfully. The compiled PDF contains the approved temporary wording. No undefined reference or citation warning was reported. Minor box warnings remain for later layout review.
