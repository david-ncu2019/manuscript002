# Suggestions for revising the calendar-fair rerun plan

Revise the plan once before execution as follows: expand its title and scope to cover the consistent regeneration of Section 4.1, Section 4.2, and Section 4.3 with the same 38 predictor variables, while preserving the existing delayed-delivery design of Section 4.1 and changing only the calendar comparison used by Sections 4.2 and 4.3; replace every incorrect reference to 35 predictors with 38 predictors; in the manuscript, do not state that lithological variables were removed, do not describe an ablation, and do not discuss earlier feature sets, but simply state that the model used 38 predictor variables, describe their physical information in plain words in Methods, and provide the exact variable names in the Appendix if needed so readers know what was actually used; keep the backstage feature-selection history only in internal provenance files, where the four excluded variables may be recorded for reproducibility; correct the plan's claim that the old evaluation periods were non-overlapping because they were different and partly nested or overlapping, and do not present the shared value of `y_true` at one month as proof of confounding, because the real problem was that the old 3-year, 5-year, and 8-year scenarios ended calibration on different dates and therefore followed different evaluation and updating calendars; use one common calibration endpoint for the three history lengths, define each calibration interval explicitly as `[training_start, training_end)`, verify that it contains exactly 36, 60, or 96 monthly MLCW observations, and record both the month index and the corresponding `MM/YYYY` dates to prevent an off-by-one error; place every new artifact under `D:\1000_SCRIPTS\004_Project003\20260427_InSAR_MLCW_v3\007_tests\014_ml_nowcast\experiments\section_pooled\run_048\supplements\manuscript_results003_no_lithology_calendar_aligned\`, with meaningful subfolders such as `scripts\`, `config\`, `results\sec4_1\`, `results\sec4_2\`, `results\sec4_3\`, `figures\main\`, `figures\supplement\`, `tables\main\`, `tables\supplement\`, `validation\`, `logs\`, and `evidence\`; do not overwrite or delete `manuscript_results001`, `manuscript_results002`, the existing P0/level1a checkpoint, or any frozen common-start outputs, and instead mark them as superseded or audit-only in the new README; do not modify the canonical scripts in a way that leaves their existing output paths active, but create clearly named versioned scripts or wrappers that require an explicit `--output-root`, refuse to run when that root resolves to a frozen folder, and obtain all shared settings from one configuration file containing the station, dataset pin, 38-variable list, common endpoint, history lengths, measurement intervals, posterior interval level, random seed, and output root; before regenerating Section 4.1, identify and document the exact fitting script that created the present P0/level1a checkpoint, demonstrate a small parity check against the existing 42-variable result, and only then create the 38-variable variant, because the current `build_sec4_1_*.py` files read an existing checkpoint and cannot themselves produce the required new model output; ensure that complete-case filtering and predictor scaling are based only on the 38 variables actually used, rather than silently retaining eligibility rules derived from the older feature set; regenerate all Section 4.1 predictions, 90% Bayesian posterior predictive intervals, performance tables, coefficient summaries, and figures from the new 38-variable fits, without changing its recurring delayed-record experiment; for Section 4.2, make the 3-year, 5-year, and 8-year calibration windows end at the same common endpoint, begin evaluation on the same date, use identical 6-month or 12-month measurement schedules, and undergo the same number of model updates over the shared evaluation period, with each newly measured MLCW endpoint added only after the predictions for that interval have been scored; for Section 4.3, fit once at the same common endpoint for all three history lengths and estimate the same subsequent monthly sequence without adding any later MLCW observations; preserve the existing sign convention `e = y_hat - y`, calculate accumulated errors directly from the corresponding monthly errors, and generate each 90% Bayesian posterior predictive interval from the Bayesian ridge fit for that scenario rather than borrowing conformal intervals or uncertainty values from another experiment; state internally that predictor records preceding `training_start` may be used only to construct legitimate lagged predictors, while no MLCW response outside the defined 3-year, 5-year, or 8-year calibration window may enter model fitting; add automated checks covering every depth section and every evaluation month, not only the first endpoint, to verify identical dates and `y_true` values across history-length scenarios, exact calibration row counts, a shared calibration endpoint, identical update dates and update counts in Section 4.2, the absence of post-endpoint MLCW responses from Section 4.3 fitting, correct score-before-update ordering, absence of all lithology-derived columns from the 38-variable matrix, deterministic reruns, and unchanged hashes for all frozen outputs; include a one-section and one-scenario preflight run before the complete computation; create `README.md` to explain the scientific purpose, experiment mapping, folder structure, output status, and artifact lineage, `RUNBOOK.md` to give exact commands and execution order, `feature_manifest.json` to record the 38 variable names, `source_provenance.json` to record input paths, dataset pin, Git commits, and source hashes, `artifact_manifest.json` to record output hashes and dependencies, `environment.txt` to record Python and package versions, `validation_report.md` to summarize every required check and its PASS or FAIL status, and timestamped log files that preserve the command, working directory, start and end times, stdout, stderr, exit code, and traceback for every run; use `D:\Programs\miniconda3\Library\envs\fafalab2\python.exe` as the documented fallback if `conda run` fails; create `methods006.tex` and `results004.tex` as recoverable successors to `methods005.tex` and `results003.tex`, but do not rewrite the approved Methods subsections from scratch, and instead preserve equations, assumptions, notation, sign convention, Bayesian posterior predictive interval explanation, labels, and approved writing while changing only the calendar design, predictor count, provenance-consistent statements, and values affected by the rerun; do not alter `dataset003.tex`, `studyarea002.tex`, or any locked baseline section; describe the 38 predictors in Methods as the variables actually used, grouped by the physical information they represent, without telling readers what was previously included or excluded; create previews before replacing Figures 5 and 6 if their diagrams must be changed to show three calibration windows converging on one common endpoint; make `results004.tex` report only selected evidence needed to answer each paired Methods experiment, use three decimal places, avoid the term `pooled`, avoid displaying every available metric, and leave section-specific interpretation and physical explanations to Discussion; let Section 4.1 use the regenerated 38-variable outputs, let Section 4.2 use one main time-series figure for the 3-year case and one compact table covering the six combinations of 3, 5, and 8 years with 6-month and 12-month measurements, and let Section 4.3 use one 2-by-3 figure for S1 through S6 showing the development of absolute cumulative error after the common endpoint together with one compact endpoint table, while moving the 5-year and 8-year time-series figures, scatter plots, full per-section tables, signed cumulative errors, running MAE and RMSE, full trajectories, and secondary diagnostics to Supplementary Materials; remove unresolved choices such as `pooled or representative section`, `A or R`, and `consider a helper` from the plan by specifying the final artifacts before execution; create a standalone `supplement.tex`, with supporting content in `sections\supplement001.tex`, independent Figure S1 and Table S1 numbering, and a reliable build sequence, while preserving `appendix001.tex` exclusively for equations and detailed data-preparation procedures; refer to supplementary items in the main manuscript using simple explicit wording unless a tested cross-document reference mechanism is deliberately added; update `main.tex` to use `methods006.tex` and `results004.tex` only after verbatim copies compile successfully; commit only explicitly targeted files at each checkpoint rather than committing the whole dirty worktree, keep analysis-code commits separate from manuscript commits, preserve the isolated `reduced_v1` branch without merging it, and do not push unless separately requested; verify both `main.tex` and `supplement.tex` using the full LaTeX build sequence, inspect undefined citations and references, duplicate labels, overfull boxes, unresolved placeholders, figure placement, stale references to superseded outputs, and the rendered PDFs, and cross-check every reported number against the newly generated CSV or Parquet source; define completion as all validation checks passing, the new output package being reproducible from its README and RUNBOOK without conversation history, frozen scripts and outputs remaining unchanged, recovery files remaining byte-identical, and the manuscript describing one internally consistent 38-predictor analysis; finally, repair the mojibake already present in the plan, including strings such as `Â§` and `â€“`, and save the revised plan as UTF-8 before giving it to the executing agent.
