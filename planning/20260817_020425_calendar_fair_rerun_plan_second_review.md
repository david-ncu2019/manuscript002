# Second review of the calendar-fair rerun plan

The revised plan is substantially better, but it should receive the following final corrections before execution so that the implementing agent does not have to make new scientific or organizational decisions: repair all mojibake first and save the plan as UTF-8, including the section symbols, multiplication signs, arrows, date ranges, and the damaged sign convention, which must read `e = \hat{y} - y`; replace the output folder name `manuscript_results003_no_lithology_calendar_aligned` with the neutral and manuscript-safe name `manuscript_results003_calendar_aligned_38predictors`, because feature-selection history belongs in internal provenance and does not need to appear in a top-level artifact path that may later be copied into manuscript notes; keep the internal provenance complete, but make the manuscript state only that 38 predictor variables were used and describe or list those variables without mentioning ablation, removal, 35 variables, or the earlier 42-variable configuration; replace the dynamic instruction to derive the feature list from the live registry on every run with a frozen 38-variable manifest created once from the already verified `without_lithology` list in `D:\1000_SCRIPTS\004_Project003\20260427_InSAR_MLCW_v3\007_tests\014_ml_nowcast\experiments\section_pooled\run_048\supplements\manuscript_results002\sec4_2_lithology_ablation_manifest.json`, record that source file's hash, require all three experiment scripts to read the frozen new manifest, and use the live registry only as a validation check that fails loudly if registry contents have drifted; correct the calendar wording because a shorter history starts later, not earlier, when all histories share one endpoint; specify the actual intervals rather than leaving the endpoint implicit, using the verified complete-record start of 05/2010 and a common exclusive calibration end of 05/2018, so the 8-year window is `[05/2010, 05/2018)`, the 5-year window is `[05/2013, 05/2018)`, the 3-year window is `[05/2015, 05/2018)`, and the first monthly estimate is for 05/2018; state that these intervals must contain exactly 96, 60, and 36 MLCW response rows respectively in every section; distinguish the common calibration endpoint from the common evaluation endpoint required for a fair 6-month-versus-12-month comparison in Section 4.2, because the old logic yields thirteen complete 6-month intervals but only six complete 12-month intervals after 05/2018; compare both sampling schedules over the same 72 monthly estimates from 05/2018 through 04/2024, ending at the shared field-measurement endpoint of 05/2024, so each history-length scenario has twelve 6-month updates or six 12-month updates, and explicitly state that update dates and update counts must match across the 3-year, 5-year, and 8-year histories within each sampling schedule, while the 6-month and 12-month schedules intentionally have different update counts; keep Section 4.3's no-update evaluation over all 80 available months from 05/2018 through 12/2024 because it does not compare two sampling frequencies; replace both uses of the undefined word `usable` in the central stories with measurable questions, namely how monthly error, endpoint error, and posterior interval coverage change with initial-record length and with 6-month versus 12-month field measurements, and remove the `WRA-operational framing` phrase because the study evaluates hypothetical monitoring conditions and should not imply that the Water Resources Agency plans to reduce field measurements; revise the folder design to comply with `D:\1000_SCRIPTS\004_Project003\20260427_InSAR_MLCW_v3\002_docs\references\project\FOLDER_ORGANIZATION_RULES.md`, especially the no-duplicate rule, by keeping the new tracked executable scripts in the canonical `D:\1000_SCRIPTS\004_Project003\20260427_InSAR_MLCW_v3\007_tests\014_ml_nowcast\scripts\` directory with meaningful `run048_tuku_...` names, storing no duplicate script copies under the results folder, and using `source_provenance.json` plus hashes and absolute or repository-relative canonical paths to trace the exact source code; retain `config\experiment_config.json` in the new result package if it is the run-specific frozen configuration, and add a root-level `STATUS.md` and `DECISIONS.md` so another agent can immediately see whether the package is PLANNED, RUNNING, FAILED, or COMPLETE and why the common-end design replaced the earlier outputs; define the three new executable interfaces explicitly, including a delayed-delivery runner for Section 4.1, a reduced-frequency runner for Section 4.2, and a permanent-stoppage runner for Section 4.3, with required `--config` and `--output-root` arguments, refusal to write into `manuscript_results001`, `manuscript_results002`, or existing checkpoints, nonzero exit status on validation failure, and no implicit fallback to old paths; change the vague `small parity check` into a concrete acceptance test using TUKU S1 and the first complete six-month delayed-delivery block, comparing the wrapper against the frozen 42-variable checkpoint for evaluation dates, observed values, predicted values, predictive standard deviations, lower and upper 90% limits, and fitted coefficients where available, with exact date equality and maximum absolute numerical difference no greater than `1e-8`; if the existing runner cannot execute that subset without changing production behavior, run the parity comparison in an isolated validation directory and record the extra cost rather than weakening the test; verify before the Section 4.1 rerun that `run048_evaluation.py` is truly the fitting source, not merely a registry-resolving entry point, by tracing its output files through the existing provenance and checkpoint schema; preserve Section 4.1's exact 23 six-month cycles, evaluation dates from 05/2013 through 10/2024, and 138 monthly estimates per section, and make any change to these dates or counts a hard failure rather than an accepted consequence of using 38 predictors; require complete-case eligibility to be computed from the frozen 38 variables while separately asserting that this does not alter the approved Section 4.1 evaluation calendar; define numerical reproducibility as equality of dates, row ordering, and categorical fields plus maximum absolute numerical difference no greater than `1e-10` between repeated reruns, rather than requiring byte-identical Parquet files whose metadata may differ, while continuing to require unchanged hashes for every pre-existing frozen file; exclude timestamps, logs, manifests that contain their own hashes, and generated environment capture times from deterministic artifact comparisons; reuse the existing temporal-leakage evidence and tests under `D:\1000_SCRIPTS\004_Project003\20260427_InSAR_MLCW_v3\007_tests\014_ml_nowcast\experiments\section_pooled\run_048\supplements\tuku_causal_preprocessing_audit\` instead of rebuilding equivalent checks, extend those tests only where the new common-end runners introduce new boundaries, and record links and source hashes in the new validation report; add explicit assertions that scalers are fitted only on each calibration window, predictor values used for a monthly estimate were available by that month, no MLCW response outside the selected 36-, 60-, or 96-month window enters initial fitting, each Section 4.2 endpoint is scored before being added, and no post-05/2018 MLCW response enters Section 4.3 fitting; specify that posterior limits are computed as the scenario-specific Bayesian ridge predictive mean plus or minus `1.645` times the scenario-specific predictive standard deviation and never from conformal results or another experiment; add `figure_source_manifest.json` in the manuscript worktree when selected vector PDFs are copied from the analysis result package into `D:\112_PROJECT_002\.worktrees\manuscript_reduced_v1\figures\`, recording source paths, destination names, hashes, generating scripts, and scenario filters, and never reference analysis-repository figures through absolute paths in LaTeX; preserve the approved visualization conventions from Section 4.1, export vector PDF for the manuscript and 300-dpi PNG previews, and require visual inspection before replacement; remove the remaining ambiguity in the Section 4.2 figure by specifying one two-part main figure for the 3-year initial record, with one part for 6-month measurements and one for 12-month measurements, both covering the same 05/2018 to 04/2024 monthly evaluation period and showing the same six depth sections with observations, monthly estimates, field-measurement endpoints, and 90% Bayesian posterior predictive intervals; define the compact Section 4.2 table now with exactly these columns: field-measurement interval, initial-record length, monthly MAE, monthly RMSE, empirical coverage of the nominal 90% interval, mean interval width, and endpoint MAE, using the same 72-month period for all six rows, placing sample counts in the caption rather than repeating them in the body, and reporting three decimal places; define the Section 4.3 main figure as a 2-by-3 S1-S6 panel of absolute cumulative signed error `A_{s,K}=|E_{s,K}|` against months since 05/2018 with separate 3-year, 5-year, and 8-year lines, define the main table as six section rows with the 80-month `A_{s,80}` values in three history-length columns, and state clearly in the caption that `A_{s,K}` is the absolute value of accumulated signed error rather than the sum of monthly absolute errors; keep deeper section-by-section diagnostics and additional metrics in the standalone Supplementary Materials as planned; add `sections/appendix002.tex` as a recoverable successor to `appendix001.tex` and point `main.tex` to it, because the active appendix currently says every scenario starts at the first complete monthly record and says predictive intervals apply only to the delayed-data analysis, both of which would contradict the corrected experiments; preserve its equations while updating the exact 38-predictor inventory, common-end window definitions, 72-month Section 4.2 comparison period, 80-month Section 4.3 period, endpoint weighting, and scenario-specific predictive-interval details; keep `appendix001.tex` byte-identical as the recovery copy; add an active-manuscript consistency audit covering `main.tex`, `methods006.tex`, `results004.tex`, `appendix002.tex`, `discuss003.tex`, and `conclusion002.tex` for stale references to 35 variables, common-start schedules, 140/116/80 unequal horizons, old snapshot boundaries, conformal intervals, and lithology as a model input, but do not modify the locked `dataset003.tex`, which already states correctly that lithology is used only as geological context; flag rather than silently rewrite any locked-file conflict; remove or revise the stale Discussion placeholder that currently says Section 4.1 cannot be compared with the other experiments because of a snapshot boundary, since the new plan intentionally regenerates all three using one dataset pin and one 38-variable set; specify that this execution updates design-dependent placeholders only and does not attempt to write the full scientific Discussion; correct the standalone build commands so `main.tex` uses `bibtex main` and `supplement.tex` uses `bibtex supplement`, followed in each case by two additional `pdflatex` passes; define Supplementary numbering commands and neutral stable labels such as `fig:supp_...` and `tab:supp_...`, and verify that any plain-text references from the main paper match the rendered S-numbering; make Step 0 list the exact three files to commit rather than saying `the two untracked files`, namely `sections/results003.tex`, `planning/20260816_123908_sec4_2_central_story_and_writing_handoff.md`, and `sections/results003_sec4_1_4_2_temp.tex`, and explicitly exclude the unrelated deleted figure, notebooks, preview directories, and other dirty files currently present in both repositories; add separate checkpoints after source-script creation, after validated numerical outputs, and after manuscript integration, with path-scoped commits and informative messages; keep the analysis branch `run048-tuku-no-update-sensitivity` and manuscript branch `reduced_v1` separate, never merge `reduced_v1`, and do not push either branch without a later explicit request; finally, narrow the Definition of Done from the entire manuscript to the active Methods, Results, Appendix, Supplementary Materials, and design-dependent cross-references covered by this plan, unless Discussion and Conclusions are explicitly added to scope, and require that another agent can identify the exact 38 variables, dates, scripts, commands, inputs, outputs, validation results, and manuscript source for every reported number using only `STATUS.md`, `DECISIONS.md`, `README.md`, `RUNBOOK.md`, the manifests, and the logs.
